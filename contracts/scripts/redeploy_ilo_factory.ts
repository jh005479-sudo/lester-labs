throw new Error(
  "Retired non-atomic ILO deployment: use scripts/deploy_post_compromise_replacements.ts; the replacement ILOFactory receives router, connector, treasury, and controller in its constructor.",
);
