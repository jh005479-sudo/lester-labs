throw new Error(
  "RETIRED: this script loads a raw private key, approves legacy tokens, and seeds the legacy router with zero slippage bounds. Review and create a fresh-stack-only liquidity plan instead.",
);
